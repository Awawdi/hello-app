pipeline {
  agent any  // This allows Jenkins to run the job on any available agent/node
  stages {
    stage('Echo Hello World') {
      steps {
        script {
          echo 'Hello World!'
        }
      }
    }
  }
}
